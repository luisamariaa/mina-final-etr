import 'package:flutter/material.dart';

class MarkedApplicants extends StatelessWidget {
  const MarkedApplicants({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: const Color.fromARGB(255, 223, 176, 231),
    );
  }
}
